import 'package:flutter/material.dart';

class GetDialog extends StatelessWidget {
  const GetDialog({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
